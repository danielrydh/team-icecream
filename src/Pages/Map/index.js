import React, { Fragment } from 'react';
import Map from '../..components/Map';

const MapView = () => {
  return (
    <Fragment>
      <Map />
    </Fragment>
  );
}

export default MapView;